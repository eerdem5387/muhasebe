{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/ensure-db-url.mjs && prisma generate && prisma migrate deploy && next build",
  "crons": [
    {
      "path": "/api/cron/school-sync",
      "schedule": "0 9 * * *"
    }
  ]
}
